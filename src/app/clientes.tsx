/** Ruta /clientes: conecta Expo Router con la pantalla del módulo Cliente. */
// Importa la pantalla completa desde la carpeta screens.
import ClientsScreen from '../../screens/clientsscreen';

// La exportación hace que Expo Router muestre este componente en la ruta.
export default ClientsScreen;
